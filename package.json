{
  "name": "Yugioh life points checker#0957",
  "version": "1.0.0",
  "description": "A Discord bot built with Node.js",
  "main": "index.js",  // Change this if your main file is named differently
  "scripts": {
    "start": "node index.js"  // Change this if your main file is named differently
  },
  "dependencies": {
    "discord.js": "^14.0.0"  // Adjust the version based on your needs
  },
  "engines": {
    "node": ">=16.9.0"  // Specify the Node.js version required
  }
}
